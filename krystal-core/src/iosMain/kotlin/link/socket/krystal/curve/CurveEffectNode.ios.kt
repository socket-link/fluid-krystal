package link.socket.krystal.curve

import androidx.compose.ui.graphics.drawscope.DrawScope

// iOS passes through to native Liquid Glass — this stub exists only to satisfy
// the expect/actual contract. It is never called at runtime on Apple platforms.

internal actual fun CurveEffectNode.updateCurveEffectIfNeeded(drawScope: DrawScope) {
    // No-op on iOS
}
